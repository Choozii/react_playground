import React, { useRef } from 'react';
import styled from '@emotion/styled';

const BottomSheet = ({ show, title, body }) => {
  return <Container></Container>;
};

export default BottomSheet;

const Container = styled.div`
  background: red;
`;
